.container.cf
  #blog
    - unless @posts.empty?
      - @posts.each do |post|
        %article.post
          %section.meta
            %time(datetime="#{post.created_at}") #{post.date}
          %section.meat
            %header
              %h1
                %a(href="/post/#{post.slug}") #{post.title}
            :markdown
              #{post.body}
            %section.comments-preview
              %a(href="#") #{post.comments.count} Comments

  = haml :sidebar
