%h1 Edit #{@work.title}

%form(action="/work/#{@work.slug}" method="POST")
  %input(type="hidden" name="_method" value="PUT")

  = haml :work_form

  %input#submit(type="submit" value="Submit") or <a href="/work">cancel</a>

