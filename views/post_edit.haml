%h1 Edit Post
%h2 #{@post.title}

%form(action="/post/" method="POST")
	%input(type="hidden" name="_method" value="PUT")
	
	= haml :post_form, :layout => false

	%input#submit(type="submit" value="Submit") or <a href="/post/#{@post.id}">cancel</a>
