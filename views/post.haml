.page.container.cf
  #blog
    %article.post
      %section.meta
        %time(datetime="#{@post.created_at}") #{@post.date}
      %section.meat
        %header
          %h1 #{@post.title}
        :markdown
          #{@post.body}

      = haml :disqus

  = haml :sidebar
